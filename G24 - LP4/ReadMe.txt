1. Please enter the input file name as a command line argument or u can also enter the input via the console if your using an IDE.
2. Please use the input format as given in the problem statement.
3. Most of the output generated by our program are very close to the expected output shared by professor, but due to Double rouding off issues, we miss the outputs by a couple of points.

We are making use of two hashmaps to ensure quick access of desc and Product IDs respectively as keys for each Hashmap.
This allows us to alter references of the actual object by editing the details of the reference itself.

Searching for a given ID's price is done on the Hashmap that which contains the ID as the key.
Similarly, searching for Items through their description is done through the Hashmap that contains the description as the key and an arraylist of items as the value.
